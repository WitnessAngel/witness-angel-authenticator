#:import AuthenticatorSelectorScreen waguilib.screens.authenticator_management.AuthenticatorSelectorScreen
#:import AuthenticatorCreationScreen waguilib.screens.authenticator_creation_form.AuthenticatorCreationScreen
# #:import SwapTransition kivy.uix.screenmanager.SwapTransition


RelativeLayout:

    ScreenManager:

        id: screen_manager
        #transition: SwapTransition()

        AuthenticatorSelectorScreen:
            id: authenticator_selector_screen_id
            name: "authenticator_selector_screen"

        AuthenticatorCreationScreen:
            id: authenticator_creation_screen_id
            name: "authenticator_creation_screen"
            selected_authenticator_dir: authenticator_selector_screen_id.selected_authenticator_dir

    WaitSpinner:
        id: wait_spinner
